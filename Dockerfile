# 1. Используем официальный образ Python 3.10
FROM python:3.10-slim

# 2. Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# 3. Копируем файл с зависимостями в контейнер
COPY requirements.txt .

# 4. Устанавливаем зависимости из файла requirements.txt
# Эта команда гарантирует установку свежих версий
RUN pip install --no-cache-dir -r requirements.txt

# 5. Копируем весь остальной код (вашего бота) в контейнер
COPY . .

# 6. Указываем команду для запуска бота при старте контейнера
#    ВАЖНО: Замените 'bot.py' на реальное имя вашего Python-файла
CMD ["python", "bot.py"]
